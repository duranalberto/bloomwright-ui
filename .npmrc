# Publish/consume via GitHub Packages (see SPEC.md §8 Distribution).
# Uncomment and set your GitHub org/user + a token in ~/.npmrc or CI env
# to publish this scoped package to GitHub Packages.
#
# @YOUR_SCOPE:registry=https://npm.pkg.github.com
# //npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
save-exact=false
